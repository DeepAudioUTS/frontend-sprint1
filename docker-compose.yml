services:
  frontend:
    build: ./deepaudio-frontend
    container_name: frontend
    ports:
      - "5173:5173"
    volumes:
      - ./deepaudio-frontend:/app
      - /app/node_modules

networks:
  shared-net:
    external: true
    name: deep-audio-network