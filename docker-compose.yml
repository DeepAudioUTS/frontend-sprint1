services:
  frontend:
    build: .
    container_name: frontend
    ports:
      - "5173:5173"
    volumes:
      - ./deepaudio-frontend:/app
      - node_modules:/app/node_modules

volumes:
  node_modules:

networks:
  shared-net:
    external: true
    name: deep-audio-network